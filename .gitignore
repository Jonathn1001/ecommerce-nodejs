node_modules

.env
.env.*

*.pem
.env*
.flaskenv*
!.env.project
!.env.vault

*.log
*audit.json

# --- monorepo additions ---
# agent scratch: plan ledgers, task briefs, review packages. Local working notes only.
.scratch/
.superpowers/
dist/
docker-compose.yml
**/generated/
# committed env templates (the existing .env* rules would otherwise ignore these)
!.env.example
!**/.env.example